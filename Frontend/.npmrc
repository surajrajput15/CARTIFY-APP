# eslint 10 is newer than eslint-plugin-react@7.37.5 supports (peer allows up to ^9.7).
# The flat-config lint rules still work correctly; skip the stale peer-range check so
# `npm ci` / `npm install` succeed on CI (Vercel) the same way they do locally.
legacy-peer-deps=true